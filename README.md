# -Linux-Shell-Script-Lab-Command-Line-Arguments-Inputs
In this lab, I learned how to pass dynamic values into Bash scripts using command-line arguments ($1, $2, etc.). The focus was on replacing hard-coded values with variables, debugging parameter order, and making scripts more reusable for real-world DevOps automation.
